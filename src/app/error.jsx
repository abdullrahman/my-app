"use client";
// import { ErrorMessage } from "./dashboard/common/notifications/successfull";
const Error = ({ error, reset }) => {
  return <div>{/* <ErrorMessage message={error.message} /> */}</div>;
};
export default Error;
